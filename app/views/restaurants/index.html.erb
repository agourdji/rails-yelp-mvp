<h1>Kelp Restaurants</h1>
<% @restaurants.each do |r| %>
<p><%= link_to r.name, restaurant_path(r) %></p>
<p><%= r.category %></p>
<p><%= r.address %></p>
<p><%= r.phone_number %></p>
<% end %>

<%= link_to "Add a new restaurant", new_restaurant_path %>
